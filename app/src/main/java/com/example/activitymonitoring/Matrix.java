package com.example.activitymonitoring;

import org.ejml.*;
import org.ejml.simple.SimpleMatrix;

//Stores the train data and
public class Matrix {
	public SimpleMatrix x_data = new SimpleMatrix(0,0);
	public  SimpleMatrix label_matrix = new SimpleMatrix(1098203,6);

}
